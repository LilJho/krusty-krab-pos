import MainSection from "../components/MainSection";
import Layout from "./Layout";

const Homepage = () => {
  return (
    <Layout>
      <MainSection />
    </Layout>
  );
};

export default Homepage;
